# OS-Proj-2

# Banker's Algorithm Project

## Description
This project implements the Banker's Algorithm for deadlock avoidance using C++.  
It reads system state (Allocation, Max, Available resources) from an input file and determines if the system is in a safe state, printing the safe sequence if it exists.

---

## How to Compile
```bash
g++ bankers.cpp -o bankers
